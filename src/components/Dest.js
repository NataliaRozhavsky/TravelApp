import React, { Component } from 'react';

class Dest extends Component {
  render() {
    return (
      <div className="Dest">
        {this.props.city}
      </div>
    );
  }
}

export default Dest;
